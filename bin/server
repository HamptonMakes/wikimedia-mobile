#!/usr/bin/env ruby
# Thin command line interface script.
# Run <tt>thin -h</tt> to get more usage.

trap("USR1") do
  puts "Trapped USR1"
end
require 'rubygems'
require 'thin'
require '../lib/thin_connection.rb'
Thin::Runner.new(ARGV).run!
